{
  "name": "instagram-clone",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "echo": "lerna run echo",
    "new-version": "lerna version --conventional-commits --yes",
    "d-server": "./deploy-to-server-DO.sh",
    "start-server": "yarn workdpace @instagram-clone/server spinup",
    "stop-server": "yarn workdpace @instagram-clone/server spindown"
  },
  "license": "MIT",
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
